This folder contains development-only mock fixtures and test helpers.

- `mock-users.ts` : user fixtures used by dev endpoints

Set `USE_MOCKS=true` and `NODE_ENV=development` to enable dev endpoints.
